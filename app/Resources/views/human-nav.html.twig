{# Human Nav #}

{## User Authenticated #}
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

    {# Left #}
    <ul id="human-nav" class="nav" style="float: left;">
        {% if null is not null %}
        {#{% if app.human is not null %}#}
            <li>Human go to:</li>
            <li><a>Pixel Farm (coming soon)</a></li>
        {% endif %}
    </ul>

    {# Right #}
    <ul id="human-nav" class="nav" style="float: right;">
        {#{% if app.human is not null %}#}
        {% if null is not null %}
            {#<i class="fa fa-user"></i> Human's name: {{ app.user.human.name }}</li>#}
        {% else %}
            <li><a class="btn btn-primary">Humanify</a></li>
        {% endif %}
    </ul>

{% endif %}